import { Auth0DecodedHash } from 'auth0-js';
declare class Auth0 {
    private logoutRedirectUri;
    private renewalThreshold;
    accessToken?: string;
    private auth0;
    private expiresAt;
    private userInfo?;
    private ignoreRenewalThreshold;
    constructor(audience: string, domain: string, clientId: string, redirectUri: string, logoutRedirectUri: string, renewalThreshold?: number);
    login(): void;
    handleCallback(): Promise<Auth0DecodedHash>;
    renewSession(): Promise<Auth0DecodedHash>;
    logout(onSuccess?: () => void): void;
    get isAuthenticated(): boolean;
    get hasValidToken(): boolean;
    get hasExpiredToken(): boolean;
    get hasExpired(): boolean;
    get email(): string | null;
    private setSession;
}
export default Auth0;
